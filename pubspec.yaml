name: flavors_yaml_merger
description: A package to help building flavored apps without the need to manually remove other flavor content from yaml file.
version: 1.0.2
repository: https://github.com/A-dalyomer/flavors_yaml_merger

executables:
  flavors_yaml_merger:
  restore_backup_yaml:

environment:
  sdk: ^3.2.5

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  yaml: ^3.1.2
  yaml_writer: ^2.0.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
